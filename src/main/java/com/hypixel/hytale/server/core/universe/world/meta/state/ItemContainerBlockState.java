package com.hypixel.hytale.server.core.universe.world.meta.state;

/**
 * Marker interface for block states that expose an item container.
 * At runtime the engine's own interface is used; this stub exists so
 * the plugin compiles when the engine types are not on the classpath.
 */
public interface ItemContainerBlockState {
}
